name: Claude Issue Handler

on:
  issues:
    types: [opened]
  issue_comment:
    types: [created]

jobs:
  claude:
    runs-on: ubuntu-latest

    steps:
      - name: Respond to Claude mention
        run: echo "Claude triggered"
